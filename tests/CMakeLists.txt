enable_testing()
ENABLE_LANGUAGE(C)
find_package(gflags REQUIRED)
add_executable(load_pie "pie_load.cpp" "${CMAKE_SOURCE_DIR}/lib/ivis_opengl/imdload.cpp")
target_include_directories(load_pie PRIVATE "${gflags_INCLUDE_DIR}")
link_directories(${gflags_dir})
target_link_libraries(load_pie gflags)

file(GLOB_RECURSE PIE_FILES "${CMAKE_SOURCE_DIR}/data/base/*.pie")
foreach(PIE_FILE ${PIE_FILES})
add_test(check_${PIE_FILE} load_pie ${PIE_FILE})
endforeach(PIE_FILE)
